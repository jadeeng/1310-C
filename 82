
#include <iostream>
using namespace std;

int Add(int x, int y){
    if (y==0)
    return x;
    else 
    return Add(x^y,(x&y)<<1);
}
int main()
{
int x, y;
cout<<"x\n";
cin>>x;
cout<<"y\n";
cin>>y;
    cout<<Add(x,y);

    return 0;
}
